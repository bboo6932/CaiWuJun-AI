# 蔡武君 AI 新版说明

发布日期：2026-08-24

## 本次版本

这是一个面向公开展示的 macOS 桌面 AI 伙伴版本。它保留应用本体、项目计划、支持二维码和说明文档，但不包含本地聊天记录、API Key、运行缓存或私人配置。

## 主要更新

- 重新构建可双击打开的 `蔡武君 AI.app`。
- 保留文本聊天、语音输入按钮、教学模式、对话窗口记忆入口和 AI 后端设置入口。
- 优化表情识别逻辑，降低普通对话误判为惊讶表情的概率。
- 增加回复结束后的表情/说话状态复位，减少嘴巴一直张开或表情卡住的问题。
- 顶部按钮区域改为更明确的图标按钮，方便切换记忆、新窗口、教学模式和 AI 设置。
- 发布包附带 `PROJECT_PLAN.md`，方便大家理解这个项目后续想做什么。

## 隐私说明

发布包不会包含：

- 用户和 AI 的本地聊天记录；
- `conversations.json`；
- `.env`、API Key、证书或私密配置；
- 构建缓存 `.build/`；
- 运行时数据库或日志。

本地聊天记录默认只保存在用户电脑：

```text
~/Library/Application Support/CaiWuJunAI/conversations.json
```

## 征求建议

欢迎对这些方向提建议：

- 表情系统怎么做得更自然；
- 语音输入和朗读怎么更像真正的 AI 伙伴；
- 教学模式应该优先支持哪些场景；
- 桌面宠物/角色 UI 应该如何变得更舒服；
- macOS 发布包和首次启动体验怎么优化。
